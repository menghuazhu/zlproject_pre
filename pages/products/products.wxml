<view class="container">

<<<<<<< HEAD
  <view class="switch_img">
    <swiper indicator-dots="{{indicatorDots}}" autoplay="{{autoplay}}" interval="{{interval}}" circular="{{circular}}" duration="{{duration}}" indicator-color="{{indicatorColor}}" indicator-active-color="{{indicatorActivecolor}}" class="swiperclass">
      <block wx:for="{{imgUrls}}" wx:key>
        <swiper-item>
          <image src="{{item}}" class="slide-image" />
        </swiper-item>
      </block>
    </swiper>
  </view>
=======
    <image class="logo" src='http://res.zhenlong.wang/crowdweb/upload/icon/3176874492115646.jpg'></image>
>>>>>>> 12462647bfd7bd932c3680617634267819fe3a3f
  
<view class="posi">  
<view class="position">
  <view class="tabTit box">
    <view class="flex1{{!tab[index]?' active':''}}" wx:for="{{tabTxt}}" wx:key="type" data-index="{{index}}" bindtap="filterTab">
      <text>{{item}}</text>
      <image src="/images/lower.png"></image>
    </view>
  </view>
  <!--筛选项-->
  <view class="tabLayer" hidden="{{tab[0]}}">
    <text class="{{whole_id==0?'active':''}}" data-id="0" data-index="0" data-txt="全部" bindtap="filter">全部</text>
    <text class="{{whole_id==item.id?'active':''}}" wx:for="{{wholeList}}" wx:key="pp" wx:for-item="item" data-id="{{item.id}}" data-index="0" data-txt="{{item.title}}" bindtap="filter">{{item.title}}</text>
  </view>
  <view class="tabLayer" hidden="{{tab[1]}}" style="left:{{left}}">
    <!-- <text class="{{tar_id==0?'active':''}}" data-id="0" data-index="1" data-txt="焦油" bindtap="filter">不限</text> -->
    <text class="{{tar_id==1?'active':''}}" data-id="1" data-index="1" bindtap="filter">从高到低</text>   
    <text class="{{tar_id==2?'active':''}}" data-id="2" data-index="1" bindtap="filter">从低到高</text>
  </view>
  <view class="tabLayer" hidden="{{tab[2]}}" style="left:{{right}}">
    <!-- <text class="{{price_id==0?'active':''}}" data-id="0" data-index="2" data-txt="价格" bindtap="filter">不限</text> -->
    <text class="{{price_id==1?'active':''}}" data-id="1" data-index="2" bindtap="filter">从高到低</text>
    <text class="{{price_id==2?'active':''}}" data-id="2" data-index="2" bindtap="filter">从低到高</text>
  </view>
  </view>
  </view>
  <!-- 列表 -->
  <view class='list'>
    <view class="list_con">
      <block wx:for='{{dataList}}' wx:key='list' wx:for-item="item">
        <view class="list_item">
          <!-- <navigator url='details?id={{item.goods_id}}'> -->
          <image class="newp_img" src="{{item.goods_id<=2?item.newp_img:item.popular_img}}"></image>
          <view class='img'>
            <image src="{{item.goods_img}}" mode="scaleToFill" />
          </view>
          <view class='info'>
            <text class='title'>{{item.goods_title}}</text>
            <!-- <view class='price'>￥{{item.goods_price}}</view>
        <view class='num'>销量{{item.goods_xiaoliang}}</view> -->
          </view>
          <view class='details'>
            <text class='details_title'>{{item.details_title}} <text class="iconfont icon-gengduo"></text></text>
          </view>
          <!-- </navigator> -->
          <view class='clear'></view>
        </view>
      </block>
    </view>
  </view>
</view>